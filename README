该工程属于 定位相关的样本增强项目

一、概述
1、 mixup.py  主要是 背景（可以以coco数据集作为样本素材）与前景（样本图片）融合
2、 perlin.py 主要是 对图片进行扭曲变换(看起来像是局部折叠一样，向真实的场景靠近)
3、 warpper.py 主要是 对图片 做透射 变换
4、 twist.py  一方面为 2 做样本挑选(并不是所有的样本都能做perlin扭曲)
             另一方面 是综合了 集成了 1、2、3中的增强方式。 故而， twist.py为该项目的入口
二、总结
twist.py为该项目的入口





